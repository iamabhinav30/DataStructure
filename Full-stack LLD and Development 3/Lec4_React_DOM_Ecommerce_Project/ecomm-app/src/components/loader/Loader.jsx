// import React from 'react';
import './Loader.css'; // Importing the external CSS

const Loader = () => {
    return (
        <div className="loader-backdrop">
            <div className="loader"></div>
        </div>
    );
}

export default Loader;
